---
const { title, publishedAt } = Astro.props;
---

<div>
  {title && <h3 class="text-xl pb-2">{title}</h3>}
  {publishedAt && <h4 class="text-sm pb-3">{publishedAt}</h4>}
  <article class="text-sm">
    <slot />
  </article>
</div>
